{% extends 'base.html.twig' %}

{% block title %}FootFans - Bienvenue{% endblock %}

{% block body %}
{% set flag = false %}

<div class="example-wrapper">

{% if app.user %}
    <h1>Hello {{ app.user.username }} :)</h1>

    {% for post in posts %}

      {% for friend in app.user.friends %}

        {% if post.author.id == friend.id %}
          {% set flag = true %}
          <div class="post-line" style="background:#3c3c3c;">
            <p> {{ post.author.username }} <br> {{ post.content }} - <span class="sm-txt">{{ post.postedAt|format_datetime(locale='fr') }}</span></p>
          </div>

        {% endif %}

      {% endfor %}
      

    {% endfor %}

    {% if not flag %}
      <p>Il n'y a pas de messages sur votre fil d'actualités :( <br> Suivez plus de personnes.</p>
    {% endif %}

{% else %}

    <h1>Bienvenue sur FootFans !</h1>

    <img style="height: 250px;" src="{{ asset('css/img/logo.png') }}" />

    <p style="text-align: justify; width: 60%; margin:0 auto;">FootFans est un projet d'études constituant un réseau social dédié aux supporters de football. Renseignez votre profil et allez à la rencontre des supporters de votre club favori, ou bien sympathisez avec les fans adverses ! Suivez les comptes qui vous intéressent, publiez des messages afin de vous créer votre communauté d'amis, et proposez ou participez à des évènements afin de faire vivre le mouvement de supporters de football. Toute l'actualité de vos clubs favoris est présente dans la rubrique Clubs. Supporter du dimanche, passionné de longue date, ou <i>ultra</i> en tribune, votre place est sur FootFans !</p>

{% endif %}

</div>

{% endblock %}

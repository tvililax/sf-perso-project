<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <link href="{{ asset('css/main.css') }}" rel="stylesheet" media="all" type="text/css"> 
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% if app.user %}
            <div class="mb-3">
                <a href="{{ path('app_logout') }}">Se déconnecter</a>
            </div>
            <ul class="main-navbar">
                <a href="{{ path('index') }}"><li>Accueil</li></a>
                <a href="{{ path('user_index') }}{{ app.user.id }}"><li>Profil</li></a>
                <a href="{{ path('club_index') }}"><li>Clubs</li></a>
            </ul>
        {% else %}
            <div class="mb-3">
                <ul>
                    <a href="{{ path('app_login') }}">
                        <li>Se connecter</li>
                    </a>   
                    <a href="{{ path('app_register') }}">
                        <li>S'inscrire</li>
                    </a>
                </ul>
            </div>
            <ul class="main-navbar">
                <a href="{{ path('app_logout') }}"><li>Accueil</li></a>
                <a href="{{ path('club_index') }}"><li>Clubs</li></a>
            </ul>
            
        {% endif %}
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
